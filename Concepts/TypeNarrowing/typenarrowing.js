"use strict";
//by using type of
//Using typeof The typeof operator checks the primitive type of a variable:
function work(value) {
    if (typeof value === "boolean") {
        console.log(`${value} is boolean value`);
    }
    else {
        console.log("it is a number");
    }
}
//
